/* 
insert into dbo.timelog (title, description, hours)
values('udemy excercises','Quizes and overall practice',4)
*/

select * from timelog

select * from dbo.Permissions

--delete from dbo.PermissionTypes where Id > 3
-- INSERT INTO [PermissionTypes] ([Description])
-- VALUES (N'Permision used by default when no other type is specified'),
-- (N'Permission used to access the coworking area'),
-- (N'Permission used to access the meeting room');

select * from dbo.PermissionTypes
